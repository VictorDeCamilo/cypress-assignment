describe("template spec", () => {
  it("passes", () => {
    cy.visit("https://example.cypress.io");
  });
});

//Adicionar um produto na lista de compra como usuário comum

//Limpar lista de compras como usuário comum

//Cadastrar novo produto como usuário administrador
